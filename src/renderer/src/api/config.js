/**
 * @file: API配置文件 (离线版本)
 */

export const API_CONFIG = {
  // 浏览器内核下载/代理检测服务地址（默认指向本地，用户可自行配置）
  BASE_URL: 'http://127.0.0.1:8787',
  TIMEOUT: 10000,
  HEADERS: {
    'Content-Type': 'application/json'
  }
}
